import React from "react";

const Header = () => {
  return (
    <nav className="navbar navbar-dark bg-primary text-center">
      <span className="navbar-brand mb-0 h1 text-center">Gallery App</span>
    </nav>
  );
};

export default Header;
